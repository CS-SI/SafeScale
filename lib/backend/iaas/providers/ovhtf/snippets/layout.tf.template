terraform {
	required_version = "{{ .TerraformRelease }}"
	required_providers {
		{{ range $i := .RequiredProviders }}
		{{ $i.Name }} = {
			source  = "{{ $i.Source }}"
			version = "{{ $i.Version }}"
		}
		{{ end }}
	}

	{{ if .ConsulBackend }}
	{{ .ConsulBackend }}
	{{ end }}
}

{{ .ProviderConfiguration }}

{{ if .ConsulBackendData }}
{{ .ConsulBackendData }}
{{ end }}

{{ .Resources }}