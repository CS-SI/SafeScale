package utils

var (
	// BaseFolder is the path of the base folder containing safescale data
	BaseFolder = "/opt/safescale"
	// BinFolder is the path of the folder containing safescale binaries
	BinFolder = BaseFolder + "/bin"
	// VarFolder ...
	VarFolder = BaseFolder + "/var"
	// LogFolder ...
	LogFolder = VarFolder + "/log"
	// TempFolder ...
	TempFolder = VarFolder + "/tmp"
	// StateFolder ...
	StateFolder = VarFolder + "/state"
)
