name: Docker Image CI (Stable)

on:
  push:
    branches: [ develop review/ut ]
  pull_request:
    branches: [ develop review/ut ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Build the Docker image
        run: docker build . --file Dockerfile.current --tag safescale-develop:$(date +%s)
