name: Legacy Docker Image

on:
  push:
    branches: [ release/v20.06 ]
  pull_request:
    branches: [ release/v20.06 ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Build the Docker image
        run: docker build . --file Dockerfile.current
